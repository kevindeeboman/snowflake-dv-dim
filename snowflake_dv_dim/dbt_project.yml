name: 'snowflake_dv_dim'
version: '1.0.0'

profile: 'snowflake_dv_dim'


model-paths: ["models"]
analysis-paths: ["analyses"]
test-paths: ["tests"]
seed-paths: ["seeds"]
macro-paths: ["macros"]
snapshot-paths: ["snapshots"]

clean-targets:
  - "target"

models:
  snowflake_dv_dim: